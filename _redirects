# ── CoinGyaan Version 2 redirects (Cloudflare Pages) ──
# Static files pass through untouched.
/assets/*  200!
/*.png     200!
/*.jpg     200!
/*.jpeg    200!
/*.gif     200!
/*.svg     200!
/*.webp    200!
/*.css     200!
/*.js      200!
/*.json    200!
/*.ico     200!
/*.xml     200!

# ── Legacy .html to clean Version 2 URLs ──
# Keep each line COMMENTED until the matching Version 2 page has real content
# and is no longer noindex. Redirecting an indexed page to an empty noindex
# page would drop it from search. Uncomment one line as you migrate each page.
# /bitcoin-sentiment-today.html   /bitcoin-outlook/    301
# /fear-greed-index-today.html    /fear-greed-index/   301
# /bitcoin.html                   /markets/bitcoin/    301
# /markets/ethereum.html          /markets/ethereum/   301
# /markets/altcoins.html          /markets/altcoins/   301
# /about.html                     /about/              301
# /contact.html                   /about/contact/      301
# guides.html is not in Version 2 nav. Keep it live, or retire it later:
# /guides.html                    /                    301

# Any remaining .html (guides, tools, advertise) keeps serving normally.
/*.html    200!
